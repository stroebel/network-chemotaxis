[project]
name = "numerical-approximation"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Jaco Stroebel", email = "jaco@gremble.xyz" }
]
requires-python = ">=3.14"
dependencies = [
    "jupyterlab>=4.6.3",
    "matplotlib>=3.11.1",
    "networkx>=3.6.1",
    "numpy>=2.5.2",
    "sympy>=1.14.0",
]

[project.scripts]
numerical-approximation = "numerical_approximation:main"

[build-system]
requires = ["uv_build>=0.12.3,<0.13.0"]
build-backend = "uv_build"
